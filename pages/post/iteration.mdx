import { Accordion, Card, ListGroup } from 'react-bootstrap'
import { Container } from '../../components/Document/Container'
import { Header } from '../../components/Header/Header'
import CodeBlock from '../../components/Document/CodeBlock'

<Header />
<Container>
  <Accordion defaultActiveKey="0">
    <Card>
      <Accordion.Toggle as={Card.Header} eventKey="0">
        Arrays
      </Accordion.Toggle>
      <Accordion.Collapse eventKey="0">
        <Card.Body>
          <CodeBlock code='C style for loop' pre="for(let i=0; i<array.length; i++){}"/>
          <CodeBlock code='for...of' pre="Array, Map, Set, arguments" list={[<kbd>const arr = [3, 5, 7];</kbd>,<kbd>for (let i of arr) // logs 3, 5, 7</kbd>]}/>
          <CodeBlock code='array.forEach(callback(value, index, array));' pre="takes callback"/>
        </Card.Body>
      </Accordion.Collapse>
    </Card>
    <Card>
      <Accordion.Toggle as={Card.Header} eventKey="1">
        Object
      </Accordion.Toggle>
      <Accordion.Collapse eventKey="1">
        <Card.Body>
          <CodeBlock code='for...in' pre="iterates over keys"/>
          <CodeBlock code='Object.entries' pre="returns array of key value pair array" list={[<kbd>&#123;item: 'Jeans', quantity: 2&#125;</kbd>,<kbd>[["item", "Jeans"], ["quantity", 2]]</kbd>]}/>
          <CodeBlock code='Object.keys' pre="returns array of keys"/>
          <CodeBlock code='Object.values' pre="returns array of values"/>
        </Card.Body>
      </Accordion.Collapse>
    </Card>
  </Accordion>
</Container>