import { Accordion, Card, ListGroup } from 'react-bootstrap'
import { Container } from '../../components/Document/Container'
import { Header } from '../../components/Header/Header'
import CodeBlock from '../../components/Document/CodeBlock'

<Header />
<Container>
  <Accordion defaultActiveKey="0">
    <Card>
      <Accordion.Toggle as={Card.Header} eventKey="0">
        push, pop, shift & unshift
      </Accordion.Toggle>
      <Accordion.Collapse eventKey="0">
        <Card.Body>
          <CodeBlock code='push(element0, ...)' pre="adds elements to the end" list={['Returns new length of the array']}/>
          <CodeBlock code='unshift(element0, ...)' pre="adds elements to the beginning" list={['Returns new length of the array']}/>
          <CodeBlock code='pop()' pre="removes last element from array" list={['Returns removed element']}/>
          <CodeBlock code='shift()' pre="removes first element" list={['Returns removed element']}/>
        </Card.Body>
      </Accordion.Collapse>
    </Card>
    <Card>
      <Accordion.Toggle as={Card.Header} eventKey="1">
        map, filter, reduce, some, every, find
      </Accordion.Toggle>
      <Accordion.Collapse eventKey="1">
        <Card.Body>
          <CodeBlock code='map((value, index, array) => { ... })' pre="creates a new array based on returned value from callback"/>
          <CodeBlock code='filter((currentValue, index, array) => { ... })' pre="creates a new array with all elements that pass the test"/>
          <CodeBlock code='reduce((accumulator, currentValue, index, array) => { ... }, initialValue)' pre="executes a reducer function on each element of the array"/>
          <CodeBlock code='some((element, index, array) => { ... } )' pre="returns true if at least one element passes the test"/>
          <CodeBlock code='every((element, index, array) => { ... } )' pre="returns true if all elements pass the test" list={[<kbd>[12, 5, 8, 130, 44].every(x => x >= 10); // false</kbd>]}/>
          <CodeBlock code='find((element, index, array) => { ... } )' pre="returns first element that passes the test" list={[<kbd>[5, 12, 8, 130, 44].find(x => x > 10); // 12</kbd>]}/>
        </Card.Body>
      </Accordion.Collapse>
    </Card>
  </Accordion>
</Container>